chown - In Linux, all files are owned by a specific user. The chown command enables you to change or transfer the ownership of a file to the specified username. For example, 
        $ chown abenezer2 file.txt will make abenezer2 as the new owner of the file.
wget - This Linux command line is very useful — you can even download files from the internet with the help of the wget command. To do so, simply type:-
        $ wget "link of file to be downloaded"
head - This linux command line display the first 5 or 10 lines of a python,text or whatever file in a readable format. To use it run simply:
        $ head file.py
tail - This linux command line unline head, this one displays the last 5 or 10 lines of a python, text or whatever file in a readable format. To use it run simply:
        $ tail file.txt
diff - This linux command line compares the contents of two files line by line. After analyzing the files, it will output the lines that do not match. 
       We often use this command when we need to make program alterations instead of rewriting the entire source code.
